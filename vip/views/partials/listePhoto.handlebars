<div class="containerglob">
    <div id="easyPaginate"> 
        {{#each AllPhotos}}
            <a href="/album/{{this.VIP_NUMERO}}"><img class="album" 
            src="/images/vip/{{this.PHOTO_ADRESSE}}" alt="{{this.PHOTO_ADRESSE}}"></a>
        {{/each}}

    </div>
</div>